# Placeholder: structure mirrors exp1 but with Program-of-Thought (emit short Python to compute).
# Implement supervisor that asks for plan -> codelets -> verifies each sub-result with the tester tool.
